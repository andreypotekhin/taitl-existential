# Implement transaction memo support for accurate before-state bi-events

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and
`Outcomes & Retrospective` must be kept up to date as work proceeds.

This plan is maintained according to `/docs/dev/auto/Plans.md`.

## Purpose / Big Picture

The library documentation says `Transit` and `Port` handlers should see `x0` as the entity state at the start of the
transaction and `x1` as the final state. Today, split-event lookup often fabricates `x0` by reusing the same live
entity instance for both sides, which makes bi-state rules blind to in-place mutation. After this change, a caller
will be able to register a transaction-local memo before mutating an object, then emit regular `update(...)` or
`delete(...)` events and have downstream `Transit` and `Port` handlers receive the real before-state snapshot.

A user will observe the change in a spec test that mutates an entity in place: without memo support, an
`OnTransit<Cat>` handler sees the same color on both sides; with memo support, it sees the original color in `t0`
and the updated color in `t1`.

## Progress

- [x] (2026-03-13 00:00Z) Reviewed `/docs/dev/auto/Plans.md`, the approved suggestion
      `/docs/dev/suggestions/approved/S03121601.Ex.memo.md`, and the current split/event pipeline.
- [x] (2026-03-13 00:00Z) Identified the key implementation seams in `Tr`, `ExistentialEvents`, `EventLogic`,
      `IndexingLogic`, `EventSplitter`, and split-event evaluation.
- [x] (2026-03-13 00:00Z) Authored this ExecPlan in
      `/docs/dev/planning/P03132601.Ex.memo.plan.md`.
- [x] (2026-03-13 00:00Z) Ran baseline validation with `mvn -q test` before implementation planning; the current
      workspace passed.
- [ ] Implement transaction-local memo storage and lifecycle cleanup.
- [ ] Expose memo registration through `Tr`, `Existential`, and `Ex`.
- [ ] Replace synthetic `(entity, entity)` before-state generation with memo-backed bi-event construction.
- [ ] Add regression/spec tests and update public documentation.
- [ ] Run `mvn -q test`, record outcomes, and move the finished plan to `docs/dev/planning/done`.

## Surprises & Discoveries

- Observation: the approved suggestion proposes `memo(snapshot, type)` overloads, but that API does not identify the
  live object whose identity should later resolve to the memo.
  Evidence: current runtime matching is identity-based via `RuntimeKey.equals(...)`, and memo lookup must therefore be
  anchored to the mutable live object reference, not only to the snapshot instance.

- Observation: synthetic bi-events are already being fabricated during split handling by reusing the same entity for
  both `t0` and `t1`.
  Evidence: `/src/main/java/com/taitl/ex/logic/evaluation/events/split_events/event_splitter/SplitEventType.java`
  emits `new Transit<>(entity, entity)` and `new Port<>(entity, entity)` for `CU`, `UD`, `CUD`, `Create`, `Update`,
  and `Delete`.

- Observation: runtime indexing currently keeps only the post-mutation entity for bi-events.
  Evidence: `/src/main/java/com/taitl/ex/logic/indexing/IndexingLogic.java` stores `RuntimeKey.valueOf(event, type,
  event.t1, fullNames)` with a TODO comment calling out the ambiguity.

## Decision Log

- Decision: base memo lookup on live-object identity and explicit type key, not on snapshot identity alone.
  Rationale: the runtime already uses identity semantics for event/entity matching, and lookup must find the memo when
  the same mutable object instance is later passed to `update(...)`, `delete(...)`, or split-event evaluation.
  Date/Author: 2026-03-13 / Codex Planner

- Decision: implement the public API as `memo(live, before, ...)`, plus transaction-id facade overloads, instead of
  the suggestion's `memo(before, ...)` shape.
  Rationale: storing a snapshot without also naming the live object is insufficient to build an identity-keyed memo
  map. This change keeps the feature implementable and unambiguous.
  Date/Author: 2026-03-13 / Codex Planner

- Decision: reject `before == live` by identity in memo registration and document that callers must pass a detached
  snapshot such as a clone or copy.
  Rationale: the library cannot prove a deep copy was made, but it can block the most common failure mode where the
  exact same instance is accidentally reused for both states.
  Date/Author: 2026-03-13 / Codex Planner

- Decision: memo data lives only for the lifetime of a single `Tr` and is cleared on `close()`.
  Rationale: memo state is transaction-scoped by design and must not leak across transactions or survive rollback or
  commit disposal.
  Date/Author: 2026-03-13 / Codex Planner

- Decision: when an elementary or combined single-entity event would be expanded into a `BiEvent` family and there
  are matching bi-event rules to evaluate, memo presence becomes mandatory and the runtime must fail fast if the memo
  is missing.
  Rationale: silently fabricating `(live, live)` defeats the meaning of `x0` and yields false-positive behavior for
  `Transit` and `Port` rules. A hard failure keeps the semantics honest and teaches the caller to opt in with
  `memo(...)` only when bi-state evaluation is actually needed.
  Date/Author: 2026-03-13 / Codex Planner

- Decision: the first registered memo for a `(live object identity, type key)` pair wins unless the same pair is
  memoized again explicitly by the caller.
  Rationale: `x0` means "state at the start of the transaction", so later automatic overwrites would silently corrupt
  the original before-state. If overwrite support is needed during implementation, it must be explicit and tested.
  Date/Author: 2026-03-13 / Codex Planner

## Outcomes & Retrospective

Planned outcome:

The implementation will make split bi-events observationally correct for in-place mutation workflows without forcing
users to emit `Transit` or `Port` manually. The main remaining risk is API ergonomics: the feature is only as useful
as the caller's ability to create detached snapshots correctly, so the implementation must pair runtime guards with
documentation and troubleshooting guidance.

Planning-time baseline:

The repository passed `mvn -q test` before any implementation work for this feature. That gives the implementer a
clean baseline and means any later failures can be treated as regressions introduced while executing this plan.

## Context and Orientation

This repository is a Java library that evaluates configured rules against events collected during an existential
transaction. A `Tr` object in `/src/main/java/com/taitl/existential/transactions/Tr.java` represents one library
transaction. Users can emit elementary events such as `Create`, `Update`, and `Delete`, or bi-state events such as
`Transit` and `Port`.

The runtime does not look up handlers only by the exact emitted event. Instead, it expands an event into a family of
matching events. That expansion is implemented in
`/src/main/java/com/taitl/ex/logic/evaluation/events/split_events/event_splitter/SplitEventType.java` and
`/src/main/java/com/taitl/ex/logic/evaluation/events/split_events/event_splitter/EventSplitter.java`. For example, an
`Update<T>` currently expands into `Transit<T>` by creating `new Transit<>(entity, entity)`. That is the core defect
this plan fixes.

Event reception starts in `/src/main/java/com/taitl/ex/core/existential/ExistentialEvents.java` and
`/src/main/java/com/taitl/ex/logic/events/EventLogic.java`. Unique encountered events are indexed through
`/src/main/java/com/taitl/ex/logic/indexing/IndexingLogic.java` into `RuntimeKey` values. `RuntimeKey` equality is
identity-based on the entity object, which is why memo storage must also be identity-based.

The static facade is `/src/main/java/com/taitl/existential/Ex.java`, the instance facade is
`/src/main/java/com/taitl/existential/Existential.java`, and transaction lookup by string id flows through
`/src/main/java/com/taitl/ex/logic/transactions/data/TrRegistry.java`.

For this plan, "memo" means a transaction-local snapshot of an entity's before-state, keyed by the mutable live object
reference and its type key. "Before-state" means the state that bi-event handlers should observe as `t0` for the
remainder of that transaction. "Detached snapshot" means a different object instance that will not mutate together with
the live object.

## Plan of Work

Milestone 1 establishes transaction-local memo storage and public API entry points.

Extend `/src/main/java/com/taitl/existential/transactions/Tr.java` with a small memo registry dedicated to this
feature. Keep the structure focused and identity-based, for example a `Map<TypeKey<?>, Map<Object, Object>>` where the
inner map is an `IdentityHashMap`. Add overloads that let callers register a memo using either `TypeKey<T>` or
`Class<T>`, and mirror the same feature through `/src/main/java/com/taitl/existential/Existential.java` and
`/src/main/java/com/taitl/existential/Ex.java` for transaction-id based access. The concrete API shape at the end of
this milestone should be:

    tr.memo(live, before, typeKey)
    tr.memo(live, before, MyType.class)
    ex.memo(live, before, typeKey, tranID)
    ex.memo(live, before, MyType.class, tranID)
    Ex.memo(live, before, typeKey, tranID)
    Ex.memo(live, before, MyType.class, tranID)

Registration must validate non-null arguments, reject `before == live`, and either preserve the original memo or fail
fast when the same pair is memoized twice. Choose one explicit rule during implementation and document it in the
`Decision Log` with matching tests.

Milestone 2 threads memo lookup into the event and split pipeline.

Introduce a focused helper in the transaction or event layer that can answer "given this live object and type key, do
we have a before-state memo?" The split pipeline should call that helper when it currently fabricates `Transit` or
`Port` from elementary or combined single-entity events. Replace synthetic `new Transit<>(entity, entity)` and
`new Port<>(entity, entity)` with memo-aware construction:

- If a memo exists for the runtime entity and split path needs a `Transit`, create `new Transit<>(before, live)`.
- If a memo exists and split path needs a `Port`, create `new Port<>(before, live)` for update-style flows and
  `new Port<>(null, live)` or `new Port<>(before, null)` for create/delete flows exactly as event semantics require.
- If no memo exists and no matching bi-event rules are going to be evaluated, skip bi-event fabrication for that path.
- If no memo exists and matching bi-event rules are present, fail fast with a dedicated memo-related exception message
  telling the caller to register memo state first.

To keep `SplitEventType` readable, do not stuff transaction lookup into that class directly. The cleaner seam is to let
`EventSplitter` or a new focused delegate transform split event templates into memo-aware runtime events because those
layers already see the `RuntimeKey` and its `entity/typeKey`.

This milestone therefore needs one more capability besides memo lookup: a way to detect whether the current split path
will actually evaluate `Transit` or `Port` rules. That check should use the same handler-resolution inputs the runtime
already uses, rather than inventing a parallel heuristic. In practice, this likely means consulting the resolved
`EventField` handlers and transaction intent handlers for the candidate bi-event split keys before deciding whether
missing memo is tolerated or exceptional.

This detection must be hot-path friendly. Do not make every elementary event perform a fresh scan across handler lists
or resolved `MultiKey` contents just to learn whether `Transit` or `Port` rules exist. The preferred design is to
record compact presence metadata while configuration indexes are being built, similar to how
`ConfigurationIndexes.intentEventTypes` already precomputes whether any intent rules exist for a given `EventType`.
Extend that idea for bi-event memo requirements, for example by tracking whether configured handler or intent indexes
contain `Transit` or `Port` rules for a given type candidate or `TypeKey`. Then the runtime memo gate becomes a cheap
lookup plus, only when needed, the normal handler resolution.

Milestone 3 aligns indexing and late-stage evaluation with the memo model.

Review `/src/main/java/com/taitl/ex/logic/indexing/IndexingLogic.java`,
`/src/main/java/com/taitl/ex/logic/evaluation/EvaluationLogic.java`, and the validation-stage traversal to ensure
memo-backed bi-events still index and replay correctly. The current TODO around choosing `event.t1` suggests the code
assumes the post-state is the runtime identity anchor. Preserve that identity anchor for lookup stability, but use the
memo snapshot only as bi-event payload. In other words, the entity identity stored in `RuntimeKey` should remain the
live object, while the `BiEvent` payload may contain `before` as `t0`.

This milestone must also introduce the missing-memo failure path in a place that has enough context to know whether
bi-event rules are relevant. The exception can be a dedicated `MemoException` or another focused subtype of
`ExistentialException`, but it must carry a remedy such as "memo state not present, use memo() to register the
before-state for this entity" and deep-link to troubleshooting if a new section is added.

Because this gate sits on a frequent path, validation work in this milestone should include a focused review that the
new metadata lookup is constant-time with respect to the number of configured rules. If the chosen structure needs a
new helper index, add it near `ConfigurationIndexes` or `EventField` rather than burying the logic inside runtime
evaluation code.

Milestone 4 adds behavioral coverage and documentation.

Add targeted unit tests around `Tr` memo registration and cleanup, split-event tests proving memo-aware `Transit` and
`Port` payloads, and end-to-end specification tests showing that a user can memo an object, mutate it in place, emit
`update(...)`, and observe correct `t0/t1` in handlers. Update `/docs/dev/Specification.md` with completed user
stories if new spec cases are added, prefixing completed items with `+` per project rules. If the feature introduces a
new error message for invalid memo usage, add a corresponding remedy entry to `/Troubleshooting.md` and deep-link to it
from the exception text.

## Concrete Steps

1. Create memo-focused tests before implementation:

   - `/src/test/java/com/taitl/existential/transactions/TrTest.java` or a new nearby test class for registration,
     duplicate handling, class/type-key overloads, and cleanup on `close()`.
   - `/src/test/java/com/taitl/ex/logic/evaluation/events/split_events/event_splitter/EventSplitterTest.java`
     for memo-aware split payloads.
   - `/src/test/java/com/taitl/ex/logic/evaluation/actions/ExecuteHandlersTest.java` if needed to prove handlers
     observe `t0` from the memo and `t1` from the live object.
   - `/src/test/java/com/taitl/existential/specs/library_usage/` for a user-story style regression test.

2. Implement transaction memo storage in:

   - `/src/main/java/com/taitl/existential/transactions/Tr.java`
   - Optional focused delegate in `/src/main/java/com/taitl/ex/logic/transactions/` if `Tr` grows too large.

3. Expose memo methods through facades in:

   - `/src/main/java/com/taitl/existential/Existential.java`
   - `/src/main/java/com/taitl/existential/Ex.java`
   - `/src/main/java/com/taitl/ex/core/existential/ExistentialEvents.java` if transaction-id routing belongs there.

4. Wire memo lookup into runtime event construction in:

   - `/src/main/java/com/taitl/ex/logic/evaluation/events/split_events/event_splitter/EventSplitter.java`
   - `/src/main/java/com/taitl/ex/logic/evaluation/events/split_events/event_splitter/SplitEventType.java`
     only for pure event-family mapping helpers, not transaction lookup itself.
   - `/src/main/java/com/taitl/ex/logic/configuration/indexes/ConfigurationIndexes.java` and
     `/src/main/java/com/taitl/ex/logic/configuration/indexes/data/EventField.java` if bi-event memo requirement
     metadata is best recorded at index-construction time.
   - `/src/main/java/com/taitl/ex/logic/evaluation/events/split_events/SplitEvent.java`,
     `/src/main/java/com/taitl/ex/logic/evaluation/intents/EvaluateIntents.java`, or a focused shared collaborator
     if that is where the code can reliably detect whether bi-event handlers/intents exist for the candidate split key.
   - `/src/main/java/com/taitl/ex/logic/indexing/IndexingLogic.java` if helper extraction is needed to keep payload
     and identity responsibilities clear.

5. Update documentation and troubleshooting if implementation introduces new user-visible behavior or errors:

   - `/docs/dev/Specification.md`
   - `/Readme.md`
   - `/Troubleshooting.md`

6. Run from repository root:

   mvn -q -Dtest=TrTest,EventSplitterTest,ExecuteHandlersTest test

7. Run full validation from repository root:

   mvn -q test

8. Update this plan's living sections with results, then move it to `docs/dev/planning/done`.

## Validation and Acceptance

Acceptance is met when all of the following are true:

- A caller can register a transaction-local memo using `Tr`, `Existential`, and `Ex` APIs with either `TypeKey<T>` or
  `Class<T>`.
- Registering a memo with the same object instance for both `live` and `before` fails fast with a clear message.
- Memo data is visible only within the target transaction and is cleared after transaction disposal.
- An in-place mutation followed by `update(live)` causes split `Transit` handling to expose `t0` as the detached
  before-state and `t1` as the current live object.
- If `update(live)`, `create(live)`, or `delete(live)` would require synthetic `Transit` or `Port` evaluation and no
  memo is registered, the runtime throws a memo-focused exception with a remedy instead of fabricating `(live, live)`.
- If no bi-event rules are applicable for that event path, missing memo does not fail unrelated single-entity rule
  evaluation.
- Determining whether bi-event rules are applicable is implemented through precomputed index metadata, not repeated
  handler-list scans on the runtime hot path.
- A create flow without a before-state still behaves as create, and a delete flow can use a memo to expose
  `Port(before, null)` semantics.
- Existing single-entity behavior remains unchanged when no memo is registered and no bi-event rules are relevant.
- `mvn -q test` passes.

One concrete acceptance scenario should exist as a spec:

    Repository root: /Users/chaos/Files/Dev/Code/taitl-existential
    Command: mvn -q -Dtest=<new spec class> test

The new spec should:

1. Configure an `OnTransit<Cat>` or `OnPort<Cat>` effect that records both colors.
2. Begin a transaction.
3. Register `memo(liveCat, clonedCat, Cat.class)`.
4. Mutate `liveCat.color` from `"Black"` to `"White"`.
5. Emit `update(liveCat)` or `delete(liveCat)`.
6. Commit or checkpoint as needed.
7. Assert that the recorded values are `"Black"` for `t0` and `"White"` or `null` for `t1`, depending on event kind.

One additional regression scenario should exist:

1. Configure a transaction with an `OnTransit<T>` or `OnPort<T>` rule reachable only through elementary event
   expansion.
2. Begin a transaction and mutate a live entity without calling `memo(...)`.
3. Emit `update(...)`, `create(...)`, or `delete(...)` as appropriate.
4. Assert that evaluation fails with the memo-focused exception and that the message tells the caller to use
   `memo(...)`.

## Idempotence and Recovery

The implementation is additive and safe to re-run. Memo storage is transaction-local and cleared by transaction
cleanup, so rerunning tests does not accumulate persistent state.

If implementation work partially lands and tests fail:

- Re-run only the new memo-focused tests first to localize whether the breakage is in API registration, split-event
  construction, or handler execution.
- If duplicate-memo semantics prove awkward, prefer failing fast over silent overwrite and record the final rule in
  `Decision Log`.
- If split-event code becomes too large, extract a focused delegate for memo-backed bi-event construction rather than
  further growing `SplitEventType`.

## Artifacts and Notes

Current incorrect synthetic payloads that this plan replaces:

    CUD(entity) -> Transit(entity, entity), Port(entity, entity)
    CU(entity)  -> Transit(entity, entity), Port(entity, entity)
    UD(entity)  -> Transit(entity, entity), Port(entity, entity)
    Update(entity) -> Transit(entity, entity)
    Create(entity) -> Port(entity, entity)
    Delete(entity) -> Port(entity, entity)

Target memo-aware payload rule:

    if memo exists for (live, typeKey):
        before = memo(live, typeKey)
        Transit  -> (before, live)
        Port update -> (before, live)
        Port delete -> (before, null)
    else if bi-event rules are relevant for this split path:
        throw memo-focused exception with remedy
    else:
        do not fabricate the bi-event path

## Interfaces and Dependencies

No new external dependency is needed. Use JDK collections already present in the codebase, especially
`IdentityHashMap`, because memo lookup must follow the same identity semantics as `RuntimeKey`.

The implementation should end with these interface-level capabilities:

- `Tr` can register and retrieve transaction-local before-state memos keyed by live object identity and type key.
- `Existential` and `Ex` can route memo registration by transaction id.
- `EventSplitter` or a dedicated collaborator can build memo-aware `BiEvent` instances from a `RuntimeKey`.
- The runtime can detect whether candidate `Transit` or `Port` split keys would actually hit configured handlers or
  intents, and can require memo state only in those cases.
- That detection is backed by precomputed configuration metadata, ideally keyed by event family and type or `TypeKey`,
  so memo enforcement does not add rule-scan overhead to every runtime event.
- Existing event lookup, indexing, and handler execution still use the live object as the runtime identity anchor.

Plan revision note: 2026-03-13 / Codex Planner. Initial ExecPlan created from
`/docs/dev/suggestions/approved/S03121601.Ex.memo.md` after reviewing the current transaction, indexing, and split
event pipeline. The plan intentionally changes the proposed API from `memo(before, ...)` to `memo(live, before, ...)`
because the original shape cannot support identity-based lookup. The plan was then updated to record baseline
validation with `mvn -q test`. It was later refined to require memo state when bi-event rules are relevant and to make
that relevance check performance-oriented via precomputed index metadata.
