### S02222602 Transaction quickstart example

Add a compact, end-to-end transaction example to /Readme.md and /docs/Usage.md to show the minimal lifecycle
(begin -> event/read/write -> commit/rollback), including op key formatting and expected failure modes. This
will reduce onboarding friction and make error messages easier to interpret in context.

- Provide a short code snippet with a single entity and one rule.
- Call out when tran ids become invalid and where to find troubleshooting entries.
