### S03102601 Centralize argument validation messages
Unify shared validation messages (null, even length, boolean) under Args/ArgPairChecks constants and reuse them
in Logger, SetMap, and other manual validations to prevent drift. Add a small test suite that asserts the
standardized messages are emitted across these helpers.
