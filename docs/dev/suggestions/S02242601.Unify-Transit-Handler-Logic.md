### S02242601 Unify port handler logic

OnPort and OnTransit duplicate condition handling, action invocation, and error wrapping, and already drifted
(e.g. action-only OnPort previously never executed). Consider extracting a small shared helper or base handler
that applies the same condition resolution rules and error wrapping, keeping the public constructors intact while
centralizing the logic and reducing future divergence.
