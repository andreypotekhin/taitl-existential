### S02242601 Unify transit handler logic

OnTransit and OnMutate duplicate condition handling, action invocation, and error wrapping, and already drifted
(e.g. action-only OnTransit previously never executed). Consider extracting a small shared helper or base handler
that applies the same condition resolution rules and error wrapping, keeping the public constructors intact while
centralizing the logic and reducing future divergence.
