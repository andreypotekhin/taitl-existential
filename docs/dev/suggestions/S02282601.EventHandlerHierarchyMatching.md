### S02282601 Event handler hierarchy matching

Event handler dispatch currently keys on the exact event class, which means handlers declared for more generic event
categories (for example On<Event> or a future OnEntityEvent) never receive subclass events. Consider adding
inheritance-aware matching (or an explicit base-event split) so generic handlers can reliably observe all derived
events without duplicating handlers for each concrete event class.
