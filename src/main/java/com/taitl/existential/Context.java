package com.taitl.existential;

public class Context
{
	/*
	 * TODO: // associate Transaction type that with Context transaction(Function<Transaction> producer){}
	 * require(Axioms<T> axioms){ ... axioms.tran = this; ... } intent(Intents<T> intents) { ... intents.tran = this;
	 * ... }
	 *
	 */
}
